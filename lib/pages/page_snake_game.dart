import 'package:flutter/material.dart';

class PageSnakeGame extends StatelessWidget {
  const PageSnakeGame({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
      appBar: AppBar(),
    );
  }
}